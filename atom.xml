---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ site.title | xml_escape }}</title>
  <link href="{{ post.url | prepend: site.baseurl | prepend: site.url }}"/>
  <link rel="self"  href="{{ "/atom.xml" | prepend: site.baseurl | prepend: site.url }}"/>
  <updated>{{ site.time | date: "%Y-%m-%dT%H:%M:%SZ" }}</updated>
  <author>
    <name>{{ site.author.name }}</name>
  </author>
  <id>{{ "/atom.xml" | prepend: site.baseurl | prepend: site.url }}</id>
  {% for post in site.posts limit:10 %}
  <entry>
    <title>{{ post.title | xml_escape }}</title>
    <link href="{{ post.url | prepend: site.baseurl | prepend: site.url }}"/>
    <id>{{ post.url | prepend: site.baseurl | prepend: site.url }}</id>
    <updated>{{ post.date | date: "%Y-%m-%dT%H:%M:%SZ" }}</updated>
    <published>{{ post.date | date: "%Y-%m-%dT%H:%M:%SZ" }}</published>
    <summary type="html"><![CDATA[ {{ post.content | split:'<!--more-->' | first }} ]]></summary>
    {% for tag in post.tags %}<category term="{{ tag | xml_escape }}" />{% endfor %}
    {% for cat in post.categories %}<category term="{{ cat | xml_escape }}" />{% endfor %}
  </entry>
  {% endfor %}
</feed>